# Admin panel password (single-app only; for split deploy set on Render backend)
ADMIN_PASSWORD=adswadi2024

# Backend API URL (required for split deploy: frontend on Vercel, backend on Render). Leave empty for single-app.
# Example: https://adswadi-api.onrender.com
NEXT_PUBLIC_API_URL=
